# 永生金主時鐘 Stage 2 實作報告

## 本輪功能

- 天氣統一為單一標準化物件：`condition`、溫度、體感、濕度、風速、降水、日夜、US AQI、更新時間、快取與過期旗標。
- 固定廣州 `23.1291, 113.2644`、`Asia/Shanghai`；Open-Meteo 預報與空氣品質 API 不需要私人金鑰。
- 濕度讀取 `relative_humidity_2m`；AQI 使用同一套 **US AQI** 映射：0–50 優、51–100 良、101–150 輕度污染、151–200 中度污染、201–300 重度污染、301+ 嚴重污染。缺失只顯示 `--`。
- 天氣首次載入即刷新，前景每 25 分鐘刷新；背景停止排程，回前景且資料超過 15 分鐘才補抓。快取過期不會冒充即時資料；AQI 請求逾時時會以 `AQI --` 完成天氣更新，不阻塞溫度與濕度。
- 金主沿用既有 PNG 幀與 `jinzhu-routine.js` 作為唯一位置、精靈與主行為控制者；移除了 `app.js` 中遺留的第二套金主控制器。
- 時鐘／留言互動使用實際元素的 `getBoundingClientRect()`，並在 resize、轉向、字體完成、卡片 ResizeObserver 時重算；不每幀讀取版面。
- 注意力模組只疊加視覺傾斜，不搶位置或動畫狀態；已含桌面游標與手機短暫觸控追蹤。
- 新增低成本閒置監聽：約 2 分鐘進入發呆、8 分鐘請求小睡、20 分鐘進入深睡（僅慢呼吸）；活動／回到前景以隨機平靜反應喚醒。
- 新增持久化的三級彩蛋池：小型 5–6 分鐘、 中型 20–35 分鐘、 大型 2 小時冷卻；最近事件去重、每日大型最多兩次，且只透過既有 Bridge 請求動作。
- 新增由天氣與時間驅動的 CSS 光影變數；分開調整背景、卡片與數字，不重建 DOM。

## 檔案

- `index.html`：載入 Stage 2 模組並更新快取版本。
- `scripts/app.js`：唯一天氣服務、快取與刷新策略；保留原有留言讀取流程。
- `scripts/jinzhu-routine.js`：唯一金主行為/位置調度器、動態錨點與閒置控制入口。
- `scripts/jinzhu-attention.js`：游標與觸控注意力裝飾層。
- `scripts/jinzhu-idle.js`：使用者活動與閒置狀態請求。
- `scripts/jinzhu-easter-eggs.js`：加權、冷卻、去重與每日限制。
- `scripts/jinzhu-lighting.js`、`styles/style.css`：天氣/時段光影。

## 留言板與素材

留言的儲存、讀取、外觀、位置與資料均未改動。互動只加在卡片外層的 class 與獨立爪印節點，從不寫入 Supabase。

沒有加入通用貓圖或 GIF。邊緣貓爪素材目前只有預留降級策略，正式頁面不顯示不匹配圖片；若日後繪製，應使用原金主白貓畫風、透明 PNG/WebP、約 220×160 px、從畫面邊緣向內伸出的單前爪。

## 優先級與狀態鎖

`jinzhu-routine.js` 是唯一可改變金主位置、精靈或主狀態的模組。特殊天氣、睡眠、直接互動、時鐘／留言互動、彩蛋與普通待機都經由它的 `JinzhuBridge` 入口；`isBusy`、行為取消計時器與各項冷卻用來阻止同時動作。注意力、閒置、彩蛋與光影模組只提出請求或修改 CSS，不直接控制金主。

## 驗證與已知項目

已通過 JavaScript 語法解析與 `git diff --check`，並確認 GitHub Pages 已提供版本 34 的 `index.html`、Stage 2 模組與報告（HTTP 200）。也以公開 Open-Meteo 回應核對了所需天氣欄位及 `us_aqi` 欄位。

自動化瀏覽器環境會由客戶端攔截外部 JavaScript，因而不能把它當作互動執行結果。仍應以 390×844、430×932、iPad 直向及桌面 Chrome/Safari 進行一次人工互動測試，尤其是旋轉後錨點與長閒置喚醒。
